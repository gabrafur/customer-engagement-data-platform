bundle:
  name: customer-engagement-demo

include:
  - resources/*.yml

artifacts:
  default:
    type: whl
    build: python -m build --wheel

targets:
  development:
    mode: development
    default: true
  production:
    mode: production
